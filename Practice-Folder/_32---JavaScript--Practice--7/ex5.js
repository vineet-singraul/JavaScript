let sentence = "Hello, welcome to the world of JavaScript!";

let substring = "JavaScript";

if (sentence.includes(substring)) {
    console.log(`The sentence contains the substring: "${substring}"`);
} else {
    console.log(`The sentence does not contain the substring: "${substring}"`);
}
